{
  "name": "naldama-mono",
  "version": "0.0.1",
  "description": "Monorepo de componentes de Tailwind para prototipado.",
  "main": "index.js",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev --no-cache --parallel --continue",
    "start": "turbo run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.4.2"
  },
  "dependencies": {
    "next-transpile-modules": "^9.0.0"
  }
}
